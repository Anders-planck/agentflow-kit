{
  "name": "orditra",
  "version": "0.1.0",
  "description": "One reversible configuration fabric for Codex, Claude Code, and OpenCode.",
  "type": "module",
  "bin": {
    "orditra": "dist/src/cli.js"
  },
  "files": [
    "dist",
    "presets",
    "registry",
    "policies",
    "skills",
    "ast-grep",
    "config",
    "docs",
    "README.md",
    "CHANGELOG.md",
    "LICENSE",
    "SECURITY.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/postbuild.mjs",
    "clean": "node scripts/clean.mjs",
    "test": "npm run build && node --test dist/test/**/*.test.js",
    "test:unit": "npm run build && node --test dist/test/unit/*.test.js",
    "check": "npm run build && npm test && node dist/src/cli.js validate --root .",
    "prepare": "npm run build"
  },
  "engines": {
    "node": ">=22.5"
  },
  "keywords": [
    "orditra",
    "codex",
    "claude-code",
    "opencode",
    "agent-skills",
    "mcp",
    "serena",
    "context-mode",
    "ast-grep"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Anders-planck/orditra.git"
  },
  "dependencies": {
    "jsonc-parser": "^3.3.1",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.9.3"
  }
}
